---
title: "Vue——Vue时间格式转化"
date: 2026-07-11
description: ""
author: "NickLinus"
category: "Vue"
thumbnail: ""
published: true
---

# Vue——Vue时间格式转化

> 原创 已于 2022-07-10 09:51:07 修改 · 948 阅读 · 1 · 2 · 本内容遵循CC 4.0 BY-SA版权协议 版权声明：本文为博主原创文章，遵循 CC 4.0 BY-SA 版权协议，转载请附上原文出处链接和本声明。
> 文章链接：https://blog.csdn.net/qq_43201350/article/details/109403836

**位置：main.js** 

```
Vue.filter('dateFormat', function (originVal) { //dateFormat为自定义函数名
    const dt = new Date(originVal)
    const y = dt.getFullYear()
    const m = (dt.getMonth() + 1 + '').padStart(2, '0')
    const d = (dt.getDate() + '').padStart(2, '0')
    const hh = (dt.getHours() + '').padStart(2, '0')
    const mm = (dt.getMinutes() + '').padStart(2, '0')
    const ss = (dt.getSeconds() + '').padStart(2, '0')
    // 时间格式年月日、时分秒
    return `${y}-${m}-${d}`
})
```

**引用：** 

```
<li class="item" v-for="(item, index) in adviceList" :key="index">
     <div>{{item.time | dateFormat }}</div>
</li>
```