---
title: "JS——JavaScript入门【 JavaScript 确认confirm 】"
description: "confirm 消息对话框通常用于允许用户做选择的动作，如：“你对吗？”等。弹出对话框(包括一个确定按钮和一个取消按钮)。"
date: "2023-07-23"
tags:
  - JavaScript
published: true
featured: true
---

> 文章链接：https://blog.csdn.net/qq_43201350/article/details/131876623

confirm 消息对话框通常用于允许用户做选择的动作，如：“你对吗？”等。弹出对话框(包括一个确定按钮和一个取消按钮)。

语法

```js
confirm(str);
```

参数说明

```js
str：在消息对话框中要显示的文本
返回值: Boolean值
```

返回值

```javascript
当用户点击"确定"按钮时，返回true
当用户点击"取消"按钮时，返回false
```

使用

```js
<script type="text/javascript">
    var message=confirm("你喜欢JavaScript吗?");
    if(message==true)
    {   document.write("很好,加油!");   }
    else
    {  document.write("JS功能强大,要学习");   }
</script>
```
